# .env CORRIGIDO PARA USO LOCAL (SEM QUEUE)

# Chave de criptografia
N8N_ENCRYPTION_KEY=kTEeqZDaX0sdEAgYOa1aoEN2Wnb0H+Lj5Z+AKaBMqrA5aUfWKhBU3pmaB9k1/sD9

# Postgres
POSTGRES_USER=n8n
POSTGRES_PASSWORD=n8n_pass
POSTGRES_DB=n8n
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis (Pode ignorar ou comentar, pois não será usado)
QUEUE_BULL_REDIS_HOST=redis
QUEUE_BULL_REDIS_PORT=6379

# Acesso local
N8N_HOST=localhost
N8N_PROTOCOL=http
WEBHOOK_URL= https://laurel-unscissored-semicomically.ngrok-free.dev

# Runtime settings
NODE_ENV=production

# --- MUDANÇA IMPORTANTE AQUI ---
# Mude de 'queue' para 'regular' (ou comente a linha para usar o padrão)
EXECUTIONS_MODE=regular

# Desligue o offload, pois não temos workers rodando
OFFLOAD_MANUAL_EXECUTIONS_TO_WORKERS=false

# Pode manter false ou true, mas sem workers não faz diferença
N8N_RUNNERS_ENABLED=false
# -------------------------------

QUEUE_HEALTH_CHECK_ACTIVE=true

N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
N8N_BLOCK_ENV_ACCESS_IN_NODE=false
N8N_GIT_NODE_DISABLE_BARE_REPOS=true

# Retenção de dados (Isso está ótimo)
EXECUTIONS_DATA_PRUNE=true
EXECUTIONS_DATA_MAX_AGE=168